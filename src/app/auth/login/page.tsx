import LoginForm from "@/src/features/auth/components/auth/LoginForm";

export default function LoginPage() {
  return (
    <div className="min-h-screen fixed flex items-center justify-center bg-slate-100">
      <LoginForm />
    </div>
  );
}