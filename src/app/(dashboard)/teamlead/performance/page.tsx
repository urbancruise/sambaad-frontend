// NOTE: "PerformanceDashboaed" is not a typo introduced here — it's the
// actual filename in your repo (features/performance/page/PerformanceDashboaed.tsx).
// Import path must match exactly or this will fail to resolve.
import PerformanceDashboard from "@/src/features/performance/page/PerformanceDashboaed";

export default function TeamLeadPerformancePage() {
    return <PerformanceDashboard />;
}
